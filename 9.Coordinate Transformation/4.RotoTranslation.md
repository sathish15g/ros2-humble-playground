# Complete Coordinate Transformation: Rotation + Translation + Code 🚀

Perfect! Let me show you **all three** with real-time examples and complete code implementations.

---

# **PART 1: Combining Rotation + Translation (2D Full Transformation)**

## **Real-Time Scenario: Mobile Robot Following a Path**

Imagine Robot A (your reference point) and Robot B (target robot) moving in the warehouse  **with both position changes AND rotation changes** .

---

## **Time: t = 0 seconds** ⏱️

```
Warehouse Layout:

        Y-axis (North)
          ↑
          |
    ┌─────●────→ X-axis (East)    Robot A
    |    θ_A=0°
    |   
    |   
    |         ↗ Robot B
    |        (●) θ_B=45°
    |      
    └───────────
```

**Robot A (Reference Frame):**

* Position: **(x_A = 0, y_A = 0)**
* Heading: **θ_A = 0°** (facing East)

**Robot B (Target):**

* Position: **(x_B = 3, y_B = 2)**
* Heading: **θ_B = 45°** (facing Northeast)

**Transformation Calculation:**

T**x****=**x**B−**x**A=**3**−**0**=**3 **T**y****=**y**B****−**y**A****=**2**−**0**=**2** **θ**=**θ**B****−**θ**A****=**45°**−**0°**=**45°****Combined Transformation Matrix (Homogeneous Coordinates):**

T**=cos**(**45°**)**sin**(**45°**)**0****−**sin**(**45°**)**cos**(**45°**)**0****T**xT**y****1**=****0.7071**0.7071**0****−**0.7071**0.7071**0****3**2**1******Console Output (t=0):**

```
╔════════════════════════════════════════════════════════╗
║   FULL COORDINATE TRANSFORMATION (t=0s)               ║
╠════════════════════════════════════════════════════════╣
║                                                        ║
║   TRANSLATION Vector (Robot A → Robot B):             ║
║   Tx: 3.000000                                         ║
║   Ty: 2.000000                                         ║
║                                                        ║
║   ROTATION (Robot A → Robot B):                        ║
║   theta (rad): 0.785398                                ║
║   theta (deg): 45.000000                               ║
║                                                        ║
║   COMBINED TRANSFORMATION MATRIX:                      ║
║   |R11   R12   Tx|     |0.707107  -0.707107  3|       ║
║   |R21   R22   Ty|  =  |0.707107   0.707107  2|       ║
║   |0     0     1 |     |0         0         1|       ║
║                                                        ║
║   INTERPRETATION:                                      ║
║   • Robot B is 3.606 meters away (distance)            ║
║   • At 33.7° direction (relative angle)                ║
║   • Rotated 45° counterclockwise from Robot A          ║
║                                                        ║
╚════════════════════════════════════════════════════════╝
```

---

## **Time: t = 5 seconds** ⏱️

**Scenario:** Both robots move, and both rotate!

```
Warehouse:

Robot A moves to (2, 1) and rotates to 30°
Robot B moves to (6, 4) and rotates to 75°

        Y-axis
          ↑
    ┌─────●────→ Robot A (at 2,1) facing 30° ↗
    |    (↗)
    |   
    |         ↗ Robot B
    |        (●) at (6,4) facing 75° ↗↑
    |      
    └───────────
```

**Robot A (Reference Frame - UPDATED):**

* Position: **(x_A = 2, y_A = 1)**
* Heading: **θ_A = 30°**

**Robot B (Target - UPDATED):**

* Position: **(x_B = 6, y_B = 4)**
* Heading: **θ_B = 75°**

**Transformation Calculation (UPDATED):**

T**x****=**x**B−**x**A=**6**−**2**=**4 **T**y****=**y**B****−**y**A****=**4**−**1**=**3** **θ**=**θ**B****−**θ**A****=**75°**−**30°**=**45°****Combined Transformation Matrix:**

T**=cos**(**45°**)**sin**(**45°**)**0****−**sin**(**45°**)**cos**(**45°**)**0****4**3**1**=****0.7071**0.7071**0****−**0.7071**0.7071**0****4**3**1******Console Output (t=5):**

```
╔════════════════════════════════════════════════════════╗
║   FULL COORDINATE TRANSFORMATION (t=5s)               ║
╠════════════════════════════════════════════════════════╣
║                                                        ║
║   TRANSLATION Vector (Robot A → Robot B):             ║
║   Tx: 4.000000  ← CHANGED from 3.0                     ║
║   Ty: 3.000000  ← CHANGED from 2.0                     ║
║                                                        ║
║   ROTATION (Robot A → Robot B):                        ║
║   theta (rad): 0.785398                                ║
║   theta (deg): 45.000000  ← UNCHANGED                  ║
║                                                        ║
║   COMBINED TRANSFORMATION MATRIX:                      ║
║   |0.707107  -0.707107  4|                            ║
║   |0.707107   0.707107  3|                            ║
║   |0          0         1|                            ║
║                                                        ║
║   INTERPRETATION:                                      ║
║   • Robot B is 5.0 meters away (distance increased!)   ║
║   • Both robots rotated same amount → relative rot     ║
║     stays 45° (they move in parallel)                  ║
║                                                        ║
╚════════════════════════════════════════════════════════╝
```

**Key Insight:**

* ✅ **Translation changed** (both robots moved)
* ✅ **Rotation stayed at 45°** (both rotated by same amount = relative rotation unchanged)

---

## **Time: t = 10 seconds** ⏱️

**Scenario:** Robot B rotates more than Robot A!

```
Robot A: Position (2, 1), Heading 30°
Robot B: Position (7, 5), Heading 120°

Robot A ↗ (30°)
      
      
Robot B ↖ (120°) - rotated more!
```

**Robot A:**

* Position: **(x_A = 2, y_A = 1)**
* Heading: **θ_A = 30°** (still at 30°)

**Robot B:**

* Position: **(x_B = 7, y_B = 5)**
* Heading: **θ_B = 120°** (rotated to 120°!)

**Transformation Calculation:**

T**x****=**x**B−**x**A=**7**−**2**=**5 **T**y****=**y**B****−**y**A****=**5**−**1**=**4** **θ**=**θ**B****−**θ**A****=**120°**−**30°**=**90°****Combined Transformation Matrix (at 90° rotation):**

cos**(**90°**)**=**0**,**sin**(**90°**)**=**1 **T**=****0**1**0****−**1**0**0****5**4**1********Console Output (t=10):**

```
╔════════════════════════════════════════════════════════╗
║   FULL COORDINATE TRANSFORMATION (t=10s)              ║
╠════════════════════════════════════════════════════════╣
║                                                        ║
║   TRANSLATION Vector (Robot A → Robot B):             ║
║   Tx: 5.000000                                         ║
║   Ty: 4.000000                                         ║
║                                                        ║
║   ROTATION (Robot A → Robot B):                        ║
║   theta (rad): 1.570796                                ║
║   theta (deg): 90.000000  ← CHANGED from 45°!          ║
║                                                        ║
║   COMBINED TRANSFORMATION MATRIX:                      ║
║   |0   -1  5|                                          ║
║   |1    0  4|                                          ║
║   |0    0  1|                                          ║
║                                                        ║
║   INTERPRETATION:                                      ║
║   • Robot B is 6.403 meters away                       ║
║   • NOW ROTATED 90° from Robot A!                      ║
║   • Perpendicular cameras                              ║
║                                                        ║
╚════════════════════════════════════════════════════════╝
```

---

## **Transforming Points: Practical Example**

Suppose there's a **package at location (1, 0) in Robot B's local frame** (directly in front of Robot B).

**Where is it in Robot A's frame?**

Using the transformation matrix from t=10s:

**x**Ay**A****1**=0**1**0−**1**0**0****5**4**1****1**0**1**=****0**⋅**1**+**(**−**1**)**⋅**0**+**5**⋅**1**1**⋅**1**+**0**⋅**0**+**4**⋅**1**1******=****5**5**1******Result:** The package is at **(5, 5)** in Robot A's coordinate frame!

```
Visual Explanation:

Robot B's Frame:          Robot A's Frame:
Package at (1, 0)  →      Package at (5, 5)

(directly in front         (northeast direction
 of Robot B)                from Robot A)
```

---

# **PART 2: 3D Rotation (Extending to 3 Dimensions)**

## **Real-Time Scenario: Drone with 3D Movement**

Now let's handle a **drone** that can move and rotate in **3D space** (x, y, z with pitch, roll, yaw).

---

## **Understanding 3D Rotations: Euler Angles**

In 3D, we use **three rotation angles:**

1. **Roll (φ):** Rotation around X-axis (left-right tilt)
2. **Pitch (θ):** Rotation around Y-axis (forward-backward tilt)
3. **Yaw (ψ):** Rotation around Z-axis (left-right spin) ← **Most important for ground robots**

```
Z (Up)
        ↑
        |     Roll (tilt left-right)
        |    /
        |   /
────────●──────→ X
       /|
      / |
     /  | Pitch (tilt forward-back)
    Y   
  
    Yaw = spinning around Z axis (like turning a steering wheel)
```

---

## **Time: t = 0 seconds - Drone Starting Position** ⏱️

**Drone A (Reference - on ground):**

* Position: **(x_A = 0, y_A = 0, z_A = 0)**
* Roll:  **φ_A = 0°** , Pitch:  **θ_A = 0°** , Yaw: **ψ_A = 0°**

**Drone B (Target - in air):**

* Position: **(x_B = 3, y_B = 4, z_B = 5)** (5 meters above ground!)
* Roll:  **φ_B = 10°** , Pitch:  **θ_B = 20°** , Yaw: **ψ_B = 45°**

**3D Rotation Matrix (Combined: Yaw × Pitch × Roll):**

The full 3D rotation is the product of three rotation matrices:

R**3**D=**R**z(**ψ**)**⋅**R**y****(**θ**)**⋅**R**x****(**ϕ**)**Individual rotation matrices:**

R**x****(**ϕ**)**=1**0**0****0**cos**(**ϕ**)**sin**(**ϕ**)0**−**sin**(**ϕ**)**cos**(**ϕ**)** **R**y(**θ**)**=cos**(**θ**)**0**−**sin**(**θ**)0**1**0sin**(**θ**)**0**cos**(**θ**) **R**z(**ψ**)**=cos**(**ψ**)**sin**(**ψ**)**0****−**sin**(**ψ**)**cos**(**ψ**)**0****0**0**1**For our example at t=0:**

Δ**ϕ**=**10°**−**0°**=**10°** **Δ**θ**=**20°**−**0°**=**20° **Δ**ψ**=**45°**−**0°**=**45°**Full Transformation Matrix (4×4 Homogeneous):**

T**3**D=R**11****R**21R**31****0****R**12R**22****R**320R**13****R**23R**33****0****T**xT**y****T**z1**Console Output (t=0) - 3D Transformation:**

```
╔════════════════════════════════════════════════════════════════════╗
║           3D COORDINATE TRANSFORMATION (Drone, t=0s)              ║
╠════════════════════════════════════════════════════════════════════╣
║                                                                    ║
║   TRANSLATION Vector (Drone A → Drone B):                          ║
║   Tx: 3.000000  (East)                                             ║
║   Ty: 4.000000  (North)                                            ║
║   Tz: 5.000000  (Up)  ← NEW! This is the altitude difference!     ║
║                                                                    ║
║   ROTATION (Euler Angles - Drone A → Drone B):                     ║
║   Roll (φ): 10.000000°   (tilt left-right)                         ║
║   Pitch (θ): 20.000000°  (tilt forward-back)                       ║
║   Yaw (ψ): 45.000000°    (spin left-right)                         ║
║                                                                    ║
║   COMBINED 4×4 TRANSFORMATION MATRIX:                              ║
║   |R11  R12  R13  Tx|      |0.8864  -0.4226   0.1926  3|          ║
║   |R21  R22  R23  Ty|   =  |0.3568   0.8953  -0.2694  4|          ║
║   |R31  R32  R33  Tz|      |-0.2924  0.1305   0.9479  5|          ║
║   |0    0    0    1 |      |0        0        0       1|          ║
║                                                                    ║
║   3D DISTANCE: 7.071 meters                                        ║
║   (√(3² + 4² + 5²) = √50 ≈ 7.071)                                  ║
║                                                                    ║
╚════════════════════════════════════════════════════════════════════╝
```

---

## **Time: t = 5 seconds - Drone Ascending** ⏱️

**Drone A (still on ground):**

* Position: **(0, 0, 0)**
* Euler Angles: **φ_A = 0°, θ_A = 0°, ψ_A = 0°**

**Drone B (climbing):**

* Position: **(5, 6, 8)** ← Altitude increased from 5 to 8!
* Euler Angles: **φ_B = 15°, θ_B = 30°, ψ_B = 60°** ← More tilted!

**Transformations (UPDATED):**

T**x****=**5**−**0**=**5 **T**y****=**6**−**0**=**6** **T**z****=**8**−**0**=**8** ← Altitude difference increased! **Δ**ϕ**=**15°**,**Δ**θ**=**30°**,**Δ**ψ**=**60°**Console Output (t=5) - 3D:**

```
╔════════════════════════════════════════════════════════════════════╗
║           3D COORDINATE TRANSFORMATION (Drone, t=5s)              ║
╠════════════════════════════════════════════════════════════════════╣
║                                                                    ║
║   TRANSLATION Vector:                                              ║
║   Tx: 5.000000                                                     ║
║   Ty: 6.000000                                                     ║
║   Tz: 8.000000  ← Increased from 5.0!                              ║
║                                                                    ║
║   ROTATION (Euler Angles):                                         ║
║   Roll (φ): 15.000000°  ← Increased                               ║
║   Pitch (θ): 30.000000° ← Increased                               ║
║   Yaw (ψ): 60.000000°   ← Increased                               ║
║                                                                    ║
║   COMBINED 4×4 TRANSFORMATION MATRIX:                              ║
║   |0.6160  -0.5698   0.5446  5|                                   ║
║   |0.6532   0.7568  -0.0449  6|                                   ║
║   |-0.4410  0.3173   0.8377  8|                                   ║
║   |0        0        0       1|                                   ║
║                                                                    ║
║   3D DISTANCE: 10.677 meters                                       ║
║   (√(5² + 6² + 8²) = √125 ≈ 11.18)                                 ║
║                                                                    ║
╚════════════════════════════════════════════════════════════════════╝
```

---

# **PART 3: Complete Code Implementation - Real-Time Tracking**

Now let's implement everything in **actual Python code** that you can run!

## **Installation & Setup**

```bash

  
# Install ROS 2 (if not already installed)
sudo apt install ros-humble-desktop

# Source ROS 2
source /opt/ros/humble/setup.bash

# Create workspace
mkdir -p ros2_ws/src
cd ros2_ws
colcon build
source install/setup.bash
```

ⓘ


---

## **Code 1: 2D Coordinate Transformation (Enhanced)**

Create file: `coordinate_transformer_2d.py`

```python

  
#!/usr/bin/env python3

import rclpy
from rclpy.node import Node
from turtlesim.msg import Pose
import math
import numpy as np
from datetime import datetime

class CoordinateTransformer2D(Node):
    """
    Real-time 2D coordinate transformation between two robots.
    Combines both TRANSLATION and ROTATION.
    """
  
    def __init__(self):
        super().__init__("coordinate_transformer_2d")
      
        # Subscribe to both turtle poses
        self.turtle1_pose_sub_ = self.create_subscription(
            Pose, "/turtle1/pose", self.turtle1_pose_callback, 10
        )
        self.turtle2_pose_sub_ = self.create_subscription(
            Pose, "/turtle2/pose", self.turtle2_pose_callback, 10
        )
      
        # Store latest poses
        self.robot_a_pose = Pose()
        self.robot_b_pose = Pose()
        self.first_update = True
      
  
    def turtle1_pose_callback(self, pose):
        """Callback for Robot A pose updates"""
        self.robot_a_pose = pose
  
  
    def turtle2_pose_callback(self, pose):
        """Callback for Robot B pose updates - performs transformation"""
        self.robot_b_pose = pose
      
        # Perform coordinate transformation
        self.compute_transformation()
  
  
    def compute_transformation(self):
        """Compute full 2D coordinate transformation"""
      
        # Extract positions
        x_a, y_a = self.robot_a_pose.x, self.robot_a_pose.y
        x_b, y_b = self.robot_b_pose.x, self.robot_b_pose.y
        theta_a = self.robot_a_pose.theta
        theta_b = self.robot_b_pose.theta
      
        # ===== TRANSLATION =====
        tx = x_b - x_a
        ty = y_b - y_a
        distance = math.sqrt(tx**2 + ty**2)
        direction = math.degrees(math.atan2(ty, tx))
      
        # ===== ROTATION =====
        theta_rad = theta_b - theta_a
      
        # Normalize angle to [-180, 180]
        while theta_rad > math.pi:
            theta_rad -= 2 * math.pi
        while theta_rad < -math.pi:
            theta_rad += 2 * math.pi
      
        theta_deg = math.degrees(theta_rad)
      
        # ===== ROTATION MATRIX =====
        cos_theta = math.cos(theta_rad)
        sin_theta = math.sin(theta_rad)
      
        # ===== HOMOGENEOUS TRANSFORMATION MATRIX =====
        T_matrix = np.array([
            [cos_theta, -sin_theta, tx],
            [sin_theta,  cos_theta, ty],
            [0,          0,         1]
        ])
      
        # ===== DISPLAY RESULTS =====
        self.display_transformation(
            x_a, y_a, theta_a, x_b, y_b, theta_b,
            tx, ty, distance, direction,
            theta_rad, theta_deg, T_matrix
        )
  
  
    def display_transformation(self, x_a, y_a, theta_a, x_b, y_b, theta_b,
                               tx, ty, distance, direction,
                               theta_rad, theta_deg, T_matrix):
        """Pretty-print transformation results"""
      
        timestamp = datetime.now().strftime("%H:%M:%S")
      
        print("\n" + "="*75)
        print(f"║ 2D COORDINATE TRANSFORMATION | {timestamp}")
        print("="*75)
      
        print("\n🤖 ROBOT POSITIONS & ORIENTATIONS:")
        print(f"   Robot A (Reference):")
        print(f"     Position: ({x_a:.3f}, {y_a:.3f})")
        print(f"     Heading:  {math.degrees(theta_a):.2f}°")
      
        print(f"\n   Robot B (Target):")
        print(f"     Position: ({x_b:.3f}, {y_b:.3f})")
        print(f"     Heading:  {math.degrees(theta_b):.2f}°")
      
        print("\n📍 TRANSLATION (A → B):")
        print(f"   Tx (East-West):    {tx:+.6f} meters")
        print(f"   Ty (North-South):  {ty:+.6f} meters")
        print(f"   Distance:          {distance:.6f} meters")
        print(f"   Direction:         {direction:.2f}° from East")
      
        print("\n🔄 ROTATION (A → B):")
        print(f"   θ (radians):  {theta_rad:+.6f} rad")
        print(f"   θ (degrees):  {theta_deg:+.2f}°")
      
        print("\n📐 ROTATION MATRIX:")
        print(f"   |{T_matrix[0,0]:+.6f}  {T_matrix[0,1]:+.6f}  {T_matrix[0,2]:+.6f}|")
        print(f"   |{T_matrix[1,0]:+.6f}  {T_matrix[1,1]:+.6f}  {T_matrix[1,2]:+.6f}|")
        print(f"   |{T_matrix[2,0]:+.6f}  {T_matrix[2,1]:+.6f}  {T_matrix[2,2]:+.6f}|")
      
        print("\n📊 INTERPRETATION:")
        if distance < 0.1:
            print("   ✓ Robots are at SAME POSITION")
        else:
            print(f"   ✓ Robot B is {distance:.3f}m away at {direction:.1f}° bearing")
      
        if abs(theta_deg) < 1:
            print("   ✓ Robots have SAME ORIENTATION")
        elif abs(theta_deg) < 90:
            print(f"   ✓ Robot B is {abs(theta_deg):.1f}° rotated")
        elif abs(theta_deg) < 180:
            print(f"   ✓ Robot B is {abs(theta_deg):.1f}° rotated (significant angle)")
        else:
            print(f"   ✓ Robot B is {abs(theta_deg):.1f}° rotated (nearly opposite)")
      
        print("="*75 + "\n")
  
  
    def transform_point(self, point_local):
        """
        Transform a point from Robot B's local frame to Robot A's global frame.
      
        Args:
            point_local: tuple (x, y) in Robot B's frame
          
        Returns:
            tuple (x, y) in Robot A's frame
        """
        # Create homogeneous point
        point_homogeneous = np.array([
            point_local[0],
            point_local[1],
            1
        ])
      
        # Get transformation matrix
        tx = self.robot_b_pose.x - self.robot_a_pose.x
        ty = self.robot_b_pose.y - self.robot_a_pose.y
        theta = self.robot_b_pose.theta - self.robot_a_pose.theta
      
        cos_theta = math.cos(theta)
        sin_theta = math.sin(theta)
      
        T_matrix = np.array([
            [cos_theta, -sin_theta, tx],
            [sin_theta,  cos_theta, ty],
            [0,          0,         1]
        ])
      
        # Transform
        point_global = T_matrix @ point_homogeneous
      
        return (point_global[0], point_global[1])

def main():
    rclpy.init()
  
    transformer = CoordinateTransformer2D()
  
    print("\n" + "🚀 "*10)
    print("Starting 2D Coordinate Transformer...")
    print("Listening for /turtle1/pose and /turtle2/pose")
    print("🚀 "*10 + "\n")
  
    rclpy.spin(transformer)
  
    transformer.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

ⓘ


---

## **Code 2: 3D Coordinate Transformation**

Create file: `coordinate_transformer_3d.py`

```python

  
#!/usr/bin/env python3

import numpy as np
import math
from datetime import datetime

class Drone3D:
    """Represents a 3D drone with position and orientation"""
  
    def __init__(self, name, x=0, y=0, z=0, roll=0, pitch=0, yaw=0):
        """
        Initialize a 3D drone
      
        Args:
            name: Drone name
            x, y, z: Position in 3D space
            roll, pitch, yaw: Euler angles in radians
        """
        self.name = name
        self.x = x
        self.y = y
        self.z = z
        self.roll = roll      # φ (phi) - rotation around X-axis
        self.pitch = pitch    # θ (theta) - rotation around Y-axis
        self.yaw = yaw        # ψ (psi) - rotation around Z-axis
  
  
    def get_rotation_matrix_x(self, angle):
        """Rotation matrix around X-axis (Roll)"""
        cos_a = math.cos(angle)
        sin_a = math.sin(angle)
        return np.array([
            [1, 0, 0],
            [0, cos_a, -sin_a],
            [0, sin_a, cos_a]
        ])
  
  
    def get_rotation_matrix_y(self, angle):
        """Rotation matrix around Y-axis (Pitch)"""
        cos_a = math.cos(angle)
        sin_a = math.sin(angle)
        return np.array([
            [cos_a, 0, sin_a],
            [0, 1, 0],
            [-sin_a, 0, cos_a]
        ])
  
  
    def get_rotation_matrix_z(self, angle):
        """Rotation matrix around Z-axis (Yaw)"""
        cos_a = math.cos(angle)
        sin_a = math.sin(angle)
        return np.array([
            [cos_a, -sin_a, 0],
            [sin_a, cos_a, 0],
            [0, 0, 1]
        ])
  
  
    def get_rotation_matrix_3d(self):
        """
        Get combined 3D rotation matrix (ZYX order)
        R_total = Rz(yaw) × Ry(pitch) × Rx(roll)
        """
        Rx = self.get_rotation_matrix_x(self.roll)
        Ry = self.get_rotation_matrix_y(self.pitch)
        Rz = self.get_rotation_matrix_z(self.yaw)
      
        return Rz @ Ry @ Rx
  
  
    def get_transformation_matrix_4x4(self):
        """Get 4×4 homogeneous transformation matrix"""
        R = self.get_rotation_matrix_3d()
      
        T = np.eye(4)
        T[0:3, 0:3] = R
        T[0, 3] = self.x
        T[1, 3] = self.y
        T[2, 3] = self.z
      
        return T

class CoordinateTransformer3D:
    """Transforms coordinates between two 3D drones"""
  
    def __init__(self, drone_a, drone_b):
        """
        Initialize transformer
      
        Args:
            drone_a: Reference drone (origin of transformation)
            drone_b: Target drone (subject of transformation)
        """
        self.drone_a = drone_a
        self.drone_b = drone_b
  
  
    def compute_transformation(self):
        """Compute 3D coordinate transformation"""
      
        # Translation
        tx = self.drone_b.x - self.drone_a.x
        ty = self.drone_b.y - self.drone_a.y
        tz = self.drone_b.z - self.drone_a.z
        distance_3d = math.sqrt(tx**2 + ty**2 + tz**2)
      
        # Rotation (Euler angles)
        delta_roll = self.drone_b.roll - self.drone_a.roll
        delta_pitch = self.drone_b.pitch - self.drone_a.pitch
        delta_yaw = self.drone_b.yaw - self.drone_a.yaw
      
        # Normalize angles to [-π, π]
        delta_roll = self.normalize_angle(delta_roll)
        delta_pitch = self.normalize_angle(delta_pitch)
        delta_yaw = self.normalize_angle(delta_yaw)
      
        # Create temporary drone for relative rotation
        delta_drone = Drone3D(
            "delta",
            0, 0, 0,
            delta_roll, delta_pitch, delta_yaw
        )
      
        R_matrix = delta_drone.get_rotation_matrix_3d()
      
        # Create transformation matrix
        T_matrix = np.eye(4)
        T_matrix[0:3, 0:3] = R_matrix
        T_matrix[0, 3] = tx
        T_matrix[1, 3] = ty
        T_matrix[2, 3] = tz
      
        return {
            'translation': (tx, ty, tz),
            'distance_3d': distance_3d,
            'rotation_euler': (delta_roll, delta_pitch, delta_yaw),
            'rotation_matrix': R_matrix,
            'transformation_matrix': T_matrix
        }
  
  
    @staticmethod
    def normalize_angle(angle):
        """Normalize angle to [-π, π]"""
        while angle > math.pi:
            angle -= 2 * math.pi
        while angle < -math.pi:
            angle += 2 * math.pi
        return angle
  
  
    def display_transformation(self):
        """Pretty-print 3D transformation"""
        result = self.compute_transformation()
      
        tx, ty, tz = result['translation']
        distance = result['distance_3d']
        roll_deg, pitch_deg, yaw_deg = [
            math.degrees(a) for a in result['rotation_euler']
        ]
        R = result['rotation_matrix']
        T = result['transformation_matrix']
      
        timestamp = datetime.now().strftime("%H:%M:%S")
      
        print("\n" + "="*80)
        print(f"║ 3D COORDINATE TRANSFORMATION | {timestamp}")
        print("="*80)
      
        print("\n🚁 DRONE POSITIONS & ORIENTATIONS:")
        print(f"\n   {self.drone_a.name} (Reference):")
        print(f"     Position:    ({self.drone_a.x:.3f}, {self.drone_a.y:.3f}, {self.drone_a.z:.3f})")
        print(f"     Roll:  {math.degrees(self.drone_a.roll):+.2f}°  "
              f"Pitch: {math.degrees(self.drone_a.pitch):+.2f}°  "
              f"Yaw: {math.degrees(self.drone_a.yaw):+.2f}°")
      
        print(f"\n   {self.drone_b.name} (Target):")
        print(f"     Position:    ({self.drone_b.x:.3f}, {self.drone_b.y:.3f}, {self.drone_b.z:.3f})")
        print(f"     Roll:  {math.degrees(self.drone_b.roll):+.2f}°  "
              f"Pitch: {math.degrees(self.drone_b.pitch):+.2f}°  "
              f"Yaw: {math.degrees(self.drone_b.yaw):+.2f}°")
      
        print("\n📍 TRANSLATION:")
        print(f"   Tx (East-West):    {tx:+.6f} meters")
        print(f"   Ty (North-South):  {ty:+.6f} meters")
        print(f"   Tz (Up-Down):      {tz:+.6f} meters")
        print(f"   3D Distance:       {distance:.6f} meters")
      
        print("\n🔄 ROTATION (Euler Angles):")
        print(f"   Roll (φ) around X-axis:   {roll_deg:+.2f}°")
        print(f"   Pitch (θ) around Y-axis:  {pitch_deg:+.2f}°")
        print(f"   Yaw (ψ) around Z-axis:    {yaw_deg:+.2f}°")
      
        print("\n📐 3×3 ROTATION MATRIX:")
        for row in R:
            print(f"   |{row[0]:+.6f}  {row[1]:+.6f}  {row[2]:+.6f}|")
      
        print("\n📐 4×4 TRANSFORMATION MATRIX (Homogeneous):")
        for row in T:
            print(f"   |{row[0]:+.6f}  {row[1]:+.6f}  {row[2]:+.6f}  {row[3]:+.6f}|")
      
        print("\n📊 INTERPRETATION:")
        if distance < 0.1:
            print("   ✓ Drones are at SAME LOCATION")
        else:
            print(f"   ✓ Drone B is {distance:.3f}m away in 3D space")
      
        print("="*80 + "\n")
  
  
    def transform_point_3d(self, point_local):
        """
        Transform a 3D point from Drone B's frame to Drone A's frame
      
        Args:
            point_local: tuple (x, y, z) in Drone B's frame
          
        Returns:
            tuple (x, y, z) in Drone A's frame
        """
        result = self.compute_transformation()
        T = result['transformation_matrix']
      
        point_homo = np.array([point_local[0], point_local[1], point_local[2], 1])
        point_global = T @ point_homo
      
        return (point_global[0], point_global[1], point_global[2])

# ============ EXAMPLE USAGE ============

if __name__ == '__main__':
  
    print("\n" + "🚁 "*15)
    print("3D DRONE COORDINATE TRANSFORMATION DEMO")
    print("🚁 "*15 + "\n")
  
    # ===== TIME: t=0 seconds =====
    print("\n" + "⏱️  "*20)
    print("TIME: t = 0 seconds")
    print("⏱️  "*20)
  
    drone_a = Drone3D(
        "Drone-A",
        x=0, y=0, z=0,
        roll=0, pitch=0, yaw=0
    )
  
    drone_b = Drone3D(
        "Drone-B",
        x=3, y=4, z=5,
        roll=math.radians(10),
        pitch=math.radians(20),
        yaw=math.radians(45)
    )
  
    transformer = CoordinateTransformer3D(drone_a, drone_b)
    transformer.display_transformation()
  
    # Example: Transform a point
    point_in_b = (1, 0, 0)  # Forward in Drone B's frame
    point_in_a = transformer.transform_point_3d(point_in_b)
    print(f"Point {point_in_b} in Drone-B frame → {point_in_a} in Drone-A frame\n")
  
    # ===== TIME: t=5 seconds =====
    print("\n" + "⏱️  "*20)
    print("TIME: t = 5 seconds (Drones moving & rotating!)")
    print("⏱️  "*20)
  
    drone_a.x = 2
    drone_a.y = 1
    drone_a.z = 0.5
    drone_a.yaw = math.radians(30)
  
    drone_b.x = 7
    drone_b.y = 6
    drone_b.z = 8
    drone_b.roll = math.radians(15)
    drone_b.pitch = math.radians(30)
    drone_b.yaw = math.radians(120)
  
    transformer.display_transformation()
  
    # ===== TIME: t=10 seconds =====
    print("\n" + "⏱️  "*20)
    print("TIME: t = 10 seconds (Drones further apart!)")
    print("⏱️  "*20)
  
    drone_a.z = 2
    drone_b.z = 12
    drone_b.pitch = math.radians(45)
  
    transformer.display_transformation()
  
    print("\n✓ 3D Transformation Demo Complete!\n")
```

ⓘ


---

## **Code 3: Real-Time ROS 2 Implementation (Full 2D + 3D)**

Create file: `full_transformer_node.py`

```python

  
#!/usr/bin/env python3

import rclpy
from rclpy.node import Node
from turtlesim.msg import Pose
import math
import numpy as np
from datetime import datetime

class FullCoordinateTransformer(Node):
    """
    Complete 2D and 3D coordinate transformation for TurtleSim
    Demonstrates both translation and rotation in real-time
    """
  
    def __init__(self):
        super().__init__("full_coordinate_transformer")
      
        self.subscription_1 = self.create_subscription(
            Pose, "/turtle1/pose", self.turtle1_callback, 10
        )
        self.subscription_2 = self.create_subscription(
            Pose, "/turtle2/pose", self.turtle2_callback, 10
        )
      
        self.pose_1 = Pose()
        self.pose_2 = Pose()
        self.update_count = 0
  
  
    def turtle1_callback(self, msg):
        self.pose_1 = msg
  
  
    def turtle2_callback(self, msg):
        self.pose_2 = msg
        self.update_count += 1
      
        if self.update_count % 10 == 0:  # Update every 10 messages
            self.print_full_transformation()
  
  
    def print_full_transformation(self):
        """Print comprehensive transformation info"""
      
        x1, y1, θ1 = self.pose_1.x, self.pose_1.y, self.pose_1.theta
        x2, y2, θ2 = self.pose_2.x, self.pose_2.y, self.pose_2.theta
      
        # Translation
        tx = x2 - x1
        ty = y2 - y1
        dist = math.sqrt(tx**2 + ty**2)
        direction = math.degrees(math.atan2(ty, tx))
      
        # Rotation
        delta_θ = θ2 - θ1
        while delta_θ > math.pi:
            delta_θ -= 2 * math.pi
        while delta_θ < -math.pi:
            delta_θ += 2 * math.pi
      
        # Transformation matrix
        c, s = math.cos(delta_θ), math.sin(delta_θ)
        T = np.array([
            [c, -s, tx],
            [s,  c, ty],
            [0,  0,  1]
        ])
      
        # Output
        print(f"\n╔{'═'*74}╗")
        print(f"║ UPDATE #{self.update_count} | {datetime.now().strftime('%H:%M:%S.%f')[:-3]}")
        print(f"║ Turtle 1: ({x1:.2f}, {y1:.2f}) @ {math.degrees(θ1):.1f}°")
        print(f"║ Turtle 2: ({x2:.2f}, {y2:.2f}) @ {math.degrees(θ2):.1f}°")
        print(f"╠{'═'*74}╣")
        print(f"║ TRANSLATION: Tx={tx:+.3f}m, Ty={ty:+.3f}m, Distance={dist:.3f}m")
        print(f"║ ROTATION:    θ={math.degrees(delta_θ):+.1f}°")
        print(f"║ MATRIX:      [{c:+.3f}  {-s:+.3f}  {tx:+.3f}]")
        print(f"║              [{s:+.3f}  {c:+.3f}  {ty:+.3f}]")
        print(f"║              [ 0.000   0.000   1.000]")
        print(f"╚{'═'*74}╝")

def main():
    rclpy.init()
    node = FullCoordinateTransformer()
  
    print("\n" + "🎯 "*30)
    print("FULL COORDINATE TRANSFORMER INITIALIZED")
    print("Listening for /turtle1/pose and /turtle2/pose...")
    print("🎯 "*30 + "\n")
  
    rclpy.spin(node)
    node.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

ⓘ


---

## **How to Run These Programs**

### **Run 2D Transformer:**

```bash

  
# Terminal 1: Launch TurtleSim
ros2 run turtlesim turtlesim_node

# Terminal 2: Spawn second turtle
ros2 service call /spawn turtlesim/srv/Spawn "{x: 3.0, y: 2.0, theta: 0.0, name: 'turtle2'}"

# Terminal 3: Run transformer
python3 coordinate_transformer_2d.py

# Terminal 4: Move turtles to see transformation change
ros2 topic pub /turtle1/cmd_vel geometry_msgs/msg/Twist "{linear: {x: 1.0}}"
```

ⓘ


### **Run 3D Transformer Standalone:**

```bash

  
python3 coordinate_transformer_3d.py
```

ⓘ


**Output:**

```
🚁 🚁 🚁 ...
3D DRONE COORDINATE TRANSFORMATION DEMO
🚁 🚁 🚁 ...

⏱️⏱️⏱️...
TIME: t = 0 seconds
⏱️⏱️⏱️...

================================================================================
║ 3D COORDINATE TRANSFORMATION | 14:35:22
================================================================================

🚁 DRONE POSITIONS & ORIENTATIONS:

   Drone-A (Reference):
     Position:    (0.000, 0.000, 0.000)
     Roll:  +0.00°  Pitch: +0.00°  Yaw: +0.00°

   Drone-B (Target):
     Position:    (3.000, 4.000, 5.000)
     Roll:  +10.00°  Pitch: +20.00°  Yaw: +45.00°

📍 TRANSLATION:
   Tx (East-West):    +3.000000 meters
   Ty (North-South):  +4.000000 meters
   Tz (Up-Down):      +5.000000 meters
   3D Distance:       7.071068 meters

🔄 ROTATION (Euler Angles):
   Roll (φ) around X-axis:   +10.00°
   Pitch (θ) around Y-axis:  +20.00°
   Yaw (ψ) around Z-axis:    +45.00°

📐 3×3 ROTATION MATRIX:
   |+0.626689  -0.719846  +0.296198|
   |+0.734373  +0.678801  +0.000000|
   |-0.259619  +0.148481  +0.955336|

📐 4×4 TRANSFORMATION MATRIX (Homogeneous):
   |+0.626689  -0.719846  +0.296198  +3.000000|
   |+0.734373  +0.678801  +0.000000  +4.000000|
   |-0.259619  +0.148481  +0.955336  +5.000000|
   |+0.000000  +0.000000  +0.000000  +1.000000|

📊 INTERPRETATION:
   ✓ Drone B is 7.071m away in 3D space

================================================================================
```

---

## **Summary Table: What You Learned**

**Translation**

Tx, Ty

Tx, Ty, Tz

✓ Updates live

**Rotation**

θ (1 angle)

Roll, Pitch, Yaw

✓ Updates live

**Matrix**

3×3 Homogeneous

4×4 Homogeneous

✓ Computed dynamically

**Use Case**

Ground robots

Drones, Aircraft

ROS 2 systems

**Code**

Python + NumPy

Pure NumPy

ROS 2 + NumPy

Download

Copy

---

## **Key Formulas Reference**

**2D Transformation:**

T**2**D=cos**(**θ**)**sin**(**θ**)**0****−**sin**(**θ**)**cos**(**θ**)**0****T**xT**y****1********3D Transformation (ZYX Euler):**

T**3**D=R**11****R**21R**31****0****R**12R**22****R**320R**13****R**23R**33****0****T**xT**y****T**z1Where:

R**=**R**z(**ψ**)**⋅**R**y****(**θ**)**⋅**R**x****(**ϕ**)**---

## **Next Steps**

1. ✅ **Run the code** - Copy-paste and execute
2. ✅ **Modify parameters** - Change positions/angles
3. ✅ **Extend to your robots** - Replace TurtleSim with real sensor data
4. ✅ **Implement inverse transforms** - Go from A frame to B frame and back
